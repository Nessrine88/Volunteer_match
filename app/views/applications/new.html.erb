<%= form_with(model: @application, url: opportunity_applications_path(@opportunity), method: :post) do |f| %>
  <%= f.hidden_field :opportunity_id, value: @opportunity.id %>
  <%= f.submit "Apply" %>
<% end %>
