<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Volunteer" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ✅ TailwindCSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- ✅ Flowbite (after Tailwind) -->
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.turbo.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>

    <%= yield :head %>

    <!-- Optional: PWA manifest (uncomment when needed) -->
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- ✅ Include application stylesheet (Turbo will track changes) -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <!-- ✅ Importmap loads Turbo + Stimulus -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="relative">
      <%= render 'shared/navbar' %>

      <div class="absolute z-50 m-10">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>

      <%= yield %>
    </main>
  </body>
</html>
